services:
  # A Docker web service
  - type: web
    name: serena-mcp
    runtime: docker
    dockerfilePath: Dockerfile.render
    plan: free
    region: oregon
    envVars:
      - key: PORT
        value: 10000
